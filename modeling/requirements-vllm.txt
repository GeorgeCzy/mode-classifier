# Minimal dependency set for the local vLLM direct-inference classifier.
# vLLM is intended for Linux GPU deployment. Install this in a fresh server
# environment when you only need modeling/scripts/predict_llm_instruct.py.
# The target server currently reports NVIDIA driver 570 / CUDA 12.8. Pin vLLM
# to a CUDA-12.8-era release instead of allowing pip to pull a newer CUDA 12.9+
# stack that requires a newer driver.
--extra-index-url https://download.pytorch.org/whl/cu128
vllm==0.10.2
transformers>=4.51
